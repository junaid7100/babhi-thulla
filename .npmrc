; Render (and any NODE_ENV=production build environment) makes `npm install`
; skip devDependencies by default. The build needs devDependencies (vite,
; typescript, tailwind) to produce dist/ — only the *runtime* should be
; production-only. Force dev deps to always install.
include=dev
