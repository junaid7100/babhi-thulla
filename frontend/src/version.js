// Bump this on every fix/edit before pushing, so the on-screen version number
// (bottom-left of the app) reflects whether the latest changes are live.
export const BUILD_NUMBER = 5;
export const APP_VERSION = `v${BUILD_NUMBER}`;
